/**
 * settings.constants.ts
 *
 * Module-level constants for the settings feature.
 * Exported so other modules (e.g. contact) can reference the same value
 * without duplicating the literal string.
 */

/** The fixed primary-key for the singleton SiteSettings row. */
export const SINGLETON_ID = 'singleton';
